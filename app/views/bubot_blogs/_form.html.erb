<%= form_with(model: bubot_blog, local: true) do |form| %>
  <% if bubot_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bubot_blog.errors.count, "error") %> prohibited this bubot_blog from being saved:</h2>

      <ul>
      <% bubot_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :bubot_blog_title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :bubot_blog_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
